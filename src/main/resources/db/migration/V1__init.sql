DROP TABLE IF EXISTS limits;
CREATE TABLE limits
(
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id INTEGER,
    value NUMERIC(14,2)
);
DO $$
BEGIN
	FOR i in 1..100 LOOP
        INSERT INTO limits(user_id, value) VALUES (i, 10000);
    END LOOP;
END$$;